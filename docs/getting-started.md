# Getting Started

Install test dependencies and run the fixture checks:

```bash
python -m pip install -e '.[dev]'
python -m pytest
```

This repo is intentionally small. It exists to test the loop architecture, not to host production product code.
